package restaurant

import (
  "testing"
)

// TestPutHandler tests the put handler by creating a fake restaurant record and
// then updating the entire record. Then fetches that record to make sure the
// fields were actually changed.
func TestPutHandler(t *testing.T) {

  // ...

}
